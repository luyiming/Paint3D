#ifndef INSTRUMENTSENUM_H
#define INSTRUMENTSENUM_H

typedef enum {
    NONE_INSTRUMENT = 0,
    PIXEL,

    // Don't use it. (Used to know count of instruments)
    INSTRUMENTS_COUNT
} InstrumentsEnum;

#endif // INSTRUMENTSENUM_H
